# framesmith/transforms/__init__.py
"""Atomic ``pl.Expr -> pl.Expr`` transforms.

Each submodule groups transforms by domain (text, names, ...). Public
re-exports live on the top-level ``framesmith`` package, not here.
"""
